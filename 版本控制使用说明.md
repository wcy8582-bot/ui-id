# UI自动化测试版本控制使用说明

## 功能概述

本功能用于支持测试代码的多版本管理，在不同的系统版本迭代时可以保留对应的测试代码版本。

## 目录结构

```
.
├── config/
│   └── module_definitions.yaml     # 包含当前版本号定义
├── src/                           # 当前版本代码
│   ├── base/
│   ├── common/
│   ├── data/
│   ├── pages/
│   └── testcase/
├── src_V1/                        # V1 版本备份
├── src_V2/                        # V2 版本备份
└── version_manager.py             # 版本管理命令行工具
```

## 配置文件

### module_definitions.yaml

在配置文件中增加了 `current_version` 字段：

```yaml
# 项目和模块定义
...

# 当前版本号
current_version: V1
```

## 命令行工具使用

### 1. 查看当前版本

```bash
python version_manager.py current
```

输出示例：
```
当前版本: V1
```

### 2. 查看所有版本列表

```bash
python version_manager.py list
```

输出示例：
```
======================================================================
版本号           版本信息           备份时间           备份人
----------------------------------------------------------------------
V1            初始版本           2026-06-02 10:00:00 admin
V2            新版本             2026-06-02 11:00:00 admin
======================================================================
共 2 个版本
```

### 3. 查看指定版本信息

```bash
python version_manager.py info V1
```

### 4. 备份当前版本

```bash
python version_manager.py backup --user admin --info "初始版本"
```

- `--user`: 备份人（必填）
- `--info`: 版本信息（选填）

### 5. 创建新版本

```bash
python version_manager.py create --version V2 --user admin
```

- `--version`: 新版本号（必填）
- `--user`: 备份人（必填）

此命令会执行两个操作：
1. 更新配置文件中的版本号为 V2
2. 将当前 src 目录备份为 src_V2

### 6. 切换版本

```bash
python version_manager.py switch V1
```

- `version`: 目标版本号（必填）

此命令会执行：
1. 更新配置文件中的版本号为目标版本号
2. 将 src 目录替换为 src_V1 的内容

⚠️ 切换完成后需要重启 app.py 才会生效！

### 7. 删除版本

```bash
python version_manager.py delete V1 --user admin
```

- `version`: 要删除的版本号（必填）
- `--user`: 删除人（必填）

此命令会执行：
1. 在数据库中将该版本标记为无效
2. 将备份文件夹重命名为 `del_src_V1_20260602100000_admin`

## 数据库变更

### test_execution 表

增加了 `version` 字段，用于记录执行的版本号。

### test_versions 表（新）

| 字段名 | 类型 | 说明 |
|-------|------|-----|
| id | INT | 主键 |
| version | VARCHAR(50) | 版本号 |
| version_info | VARCHAR(500) | 版本信息 |
| backup_time | DATETIME | 备份时间 |
| backup_user | VARCHAR(100) | 备份人 |
| is_active | TINYINT | 是否有效（默认1） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

## 工作流示例

### 场景一：系统升级，旧版本仍需支持

1. 当前版本 V1，系统准备升级到 V2
2. 备份当前版本（防止直接修改）：
   ```bash
   python version_manager.py backup --user admin --info "系统V1版本"
   ```
3. 创建 V2 版本：
   ```bash
   python version_manager.py create --version V2 --user admin
   ```
4. 现在可以修改 src 目录下的代码，适配 V2 系统
5. 后续需要测试 V1 系统时：
   ```bash
   python version_manager.py switch V1
   ```
   然后重启 app.py，执行 V1 对应的测试用例

### 场景二：修复 V1 中的测试用例

1. 切换到 V1 版本：
   ```bash
   python version_manager.py switch V1
   ```
2. 修复测试用例
3. 备份修复后的 V1 版本（覆盖）：
   ```bash
   python version_manager.py backup --user admin --info "修复了V1中的bug"
   ```

## 注意事项

1. **切换版本后重启 app.py**：版本号和代码都已更改，需要重启
2. **不要手动修改备份文件夹**：`src_V1` 等文件夹内容不要手动修改
3. **删除操作不可逆**：删除前请仔细确认
4. **版本号命名规范**：建议使用 V1、V2 等规范命名，不要使用特殊字符

## 初始化新环境步骤

1. 确保已经有 database.yml 等配置
2. 运行 rebuild_db.py 重建数据库表（如果还没有）
3. 设置初始版本号在 module_definitions.yaml 中
4. 备份初始版本：
   ```bash
   python version_manager.py backup --user admin --info "初始版本"
   ```

## 常见问题

### Q: 找不到 get_current_version 函数？

检查是否导入了 module:
```python
from src.common.version_control import get_current_version
```

### Q: 数据库连接失败？

确保配置文件 execution_config.yaml 中的 database 配置正确。

### Q: 切换版本后测试用例不更新？

检查是否重启了 app.py。

